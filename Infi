------------------------------------------------
-- SERVICES / VARS
------------------------------------------------
local Players = game:GetService("Players")
local Run = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local player = Players.LocalPlayer

------------------------------------------------
-- CONFIG
------------------------------------------------
local Config = {
    WalkSpeedEnabled = false,
    WalkSpeedValue = 16,
    Language = "PT"
}

------------------------------------------------
-- GUI (PRETO / BOTÃO VERMELHO)
------------------------------------------------
local gui = Instance.new("ScreenGui")
gui.Name = "WalkMasterHub"
gui.Parent = player:WaitForChild("PlayerGui") -- Persistente

-- FRAME ÚNICO (TOPO + CORPO)
local frame = Instance.new("Frame", gui)
frame.Size = UDim2.fromScale(0.25,0.25)
frame.Position = UDim2.fromScale(0.05,0.3)
frame.BackgroundColor3 = Color3.fromRGB(0,0,0)
frame.BorderSizePixel = 0

-- TOP + TITULO
local title = Instance.new("TextLabel", frame)
title.Size = UDim2.fromScale(1,0.2)
title.Text = "WalkMaster ~ Speed"
title.TextColor3 = Color3.new(1,1,1)
title.TextScaled = true
title.BackgroundColor3 = Color3.fromRGB(180,0,0) -- Vermelho único
title.BorderSizePixel = 0

-- BOTÃO MINIMIZAR
local minBtn = Instance.new("TextButton", title)
minBtn.Size = UDim2.fromScale(0.15,1)
minBtn.Position = UDim2.fromScale(0.7,0)
minBtn.Text = "-"
minBtn.TextScaled = true
minBtn.BackgroundColor3 = Color3.fromRGB(120,0,0)
minBtn.TextColor3 = Color3.new(1,1,1)

-- BOTÃO FECHAR
local closeBtn = Instance.new("TextButton", title)
closeBtn.Size = UDim2.fromScale(0.15,1)
closeBtn.Position = UDim2.fromScale(0.85,0)
closeBtn.Text = "X"
closeBtn.TextScaled = true
closeBtn.BackgroundColor3 = Color3.fromRGB(200,0,0)
closeBtn.TextColor3 = Color3.new(1,1,1)

-- BOTÃO WALK SPEED
local btn = Instance.new("TextButton", frame)
btn.Size = UDim2.fromScale(0.4,0.2)
btn.Position = UDim2.fromScale(0.05,0.25)
btn.Text = "OFF"
btn.TextColor3 = Color3.new(1,1,1)
btn.BackgroundColor3 = Color3.fromRGB(200,0,0)
btn.TextScaled = true

-- CAIXA DE VELOCIDADE
local spBox = Instance.new("TextBox", frame)
spBox.Size = UDim2.fromScale(0.5,0.2)
spBox.Position = UDim2.fromScale(0.45,0.25)
spBox.Text = "16"
spBox.BackgroundColor3 = Color3.fromRGB(30,30,30)
spBox.TextColor3 = Color3.new(1,1,1)
spBox.TextScaled = true
spBox.ClearTextOnFocus = false

-- Bloquear letras, aceitar apenas números
spBox:GetPropertyChangedSignal("Text"):Connect(function()
	local t = spBox.Text
	local clean = t:gsub("%D","")
	if clean == "" then clean = "0" end
	spBox.Text = clean
	spBox.TextColor3 = Color3.new(1,1,1)
end)

------------------------------------------------
-- BOTÃO MINI
------------------------------------------------
local minimized = false
local miniBtn = Instance.new("TextButton", gui)
miniBtn.Size = UDim2.fromOffset(30,30)
miniBtn.Position = UDim2.fromScale(0.05,0.3)
miniBtn.Text = "+"
miniBtn.TextScaled = true
miniBtn.BackgroundColor3 = Color3.fromRGB(120,0,0)
miniBtn.TextColor3 = Color3.new(1,1,1)
miniBtn.Visible = false

------------------------------------------------
-- CONFIGURAÇÃO DE LINGUAGEM
------------------------------------------------
local configBtn = Instance.new("TextButton", frame)
configBtn.Size = UDim2.fromScale(0.9,0.15)
configBtn.Position = UDim2.fromScale(0.05,0.55)
configBtn.Text = "Config"
configBtn.BackgroundColor3 = Color3.fromRGB(100,100,100)
configBtn.TextColor3 = Color3.new(1,1,1)
configBtn.TextScaled = true

local configSec = Instance.new("Frame", frame)
configSec.Size = UDim2.fromScale(0.9,0.2)
configSec.Position = UDim2.fromScale(0.05,0.75)
configSec.BackgroundColor3 = Color3.fromRGB(20,20,20)
configSec.Visible = false

local enBtn = Instance.new("TextButton", configSec)
enBtn.Size = UDim2.fromScale(0.48,0.4)
enBtn.Position = UDim2.fromScale(0,0)
enBtn.Text = "English"
enBtn.BackgroundColor3 = Color3.fromRGB(0,150,0)
enBtn.TextColor3 = Color3.new(1,1,1)
enBtn.TextScaled = true

local ptBtn = Instance.new("TextButton", configSec)
ptBtn.Size = UDim2.fromScale(0.48,0.4)
ptBtn.Position = UDim2.fromScale(0.52,0)
ptBtn.Text = "Português"
ptBtn.BackgroundColor3 = Color3.fromRGB(0,0,150)
ptBtn.TextColor3 = Color3.new(1,1,1)
ptBtn.TextScaled = true

local function updateLanguage(lang)
	Config.Language = lang
	if lang == "EN" then
		title.Text = "WalkMaster ~ Speed"
		btn.Text = Config.WalkSpeedEnabled and "ON" or "OFF"
		spBox.PlaceholderText = "Speed (ex: 16)"
		minBtn.Text = minimized and "+" or "-"
		closeBtn.Text = "X"
		configBtn.Text = "Config"
		enBtn.Text = "English"
		ptBtn.Text = "Portuguese"
	elseif lang == "PT" then
		title.Text = "WalkMaster ~ Speed"
		btn.Text = Config.WalkSpeedEnabled and "LIGADO" or "DESLIGADO"
		spBox.PlaceholderText = "Velocidade (ex: 16)"
		minBtn.Text = minimized and "+" or "-"
		closeBtn.Text = "X"
		configBtn.Text = "Config"
		enBtn.Text = "Inglês"
		ptBtn.Text = "Português"
	end
end

enBtn.MouseButton1Click:Connect(function() updateLanguage("EN") end)
ptBtn.MouseButton1Click:Connect(function() updateLanguage("PT") end)
configBtn.MouseButton1Click:Connect(function() configSec.Visible = not configSec.Visible end)

------------------------------------------------
-- DRAG COM LIMITE (FRAME E MINI)
------------------------------------------------
local dragging, dragStart, startPos
local function setupDrag(f)
	f.InputBegan:Connect(function(i)
		if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = i.Position
			startPos = f.Position
		end
	end)
	UIS.InputChanged:Connect(function(i)
		if dragging and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
			local delta = i.Position - dragStart
			f.Position = UDim2.new(
				startPos.X.Scale, startPos.X.Offset + delta.X,
				startPos.Y.Scale, startPos.Y.Offset + delta.Y
			)
			local vp = workspace.CurrentCamera.ViewportSize
			f.Position = UDim2.fromOffset(
				math.clamp(f.AbsolutePosition.X,0,vp.X-f.AbsoluteSize.X),
				math.clamp(f.AbsolutePosition.Y,0,vp.Y-f.AbsoluteSize.Y)
			)
		end
	end)
end

setupDrag(frame)
setupDrag(miniBtn)

------------------------------------------------
-- BOTÕES MINIMIZAR E FECHAR
------------------------------------------------
minBtn.MouseButton1Click:Connect(function()
	minimized = not minimized
	frame.Visible = not minimized
	miniBtn.Visible = minimized
end)

miniBtn.MouseButton1Click:Connect(function()
	minimized = false
	frame.Visible = true
	miniBtn.Visible = false
end)

closeBtn.MouseButton1Click:Connect(function()
	gui:Destroy()
end)

------------------------------------------------
-- WALK SPEED
------------------------------------------------
btn.MouseButton1Click:Connect(function()
	Config.WalkSpeedEnabled = not Config.WalkSpeedEnabled
	btn.Text = (Config.Language == "PT") and (Config.WalkSpeedEnabled and "LIGADO" or "DESLIGADO")
		or (Config.WalkSpeedEnabled and "ON" or "OFF")
	Config.WalkSpeedValue = tonumber(spBox.Text) or Config.WalkSpeedValue
end)

local function updateWalkSpeed(humanoid)
	Run.RenderStepped:Connect(function()
		if Config.WalkSpeedEnabled and humanoid then
			humanoid.WalkSpeed = Config.WalkSpeedValue
		end
	end)
end

if player.Character then
	local hum = player.Character:WaitForChild("Humanoid")
	updateWalkSpeed(hum)
end

player.CharacterAdded:Connect(function(char)
	local hum = char:WaitForChild("Humanoid")
	updateWalkSpeed(hum)
end)

------------------------------------------------
-- TAG "DONO" PARA PLAYER ESPECÍFICO
------------------------------------------------
Run.RenderStepped:Connect(function()
	for _, plr in pairs(Players:GetPlayers()) do
		if plr.Name == "fhatom2" and plr.Character and plr.Character:FindFirstChild("Head") then
			if not plr.Character.Head:FindFirstChild("DonoTag") then
				local tag = Instance.new("BillboardGui", plr.Character.Head)
				tag.Name = "DonoTag"
				tag.Size = UDim2.new(0,100,0,50)
				tag.Adornee = plr.Character.Head
				tag.AlwaysOnTop = true

				local text = Instance.new("TextLabel", tag)
				text.Size = UDim2.fromScale(1,1)
				text.BackgroundTransparency = 1
				text.TextScaled = true
				text.Font = Enum.Font.GothamBold
				text.Text = "DONO"
				text.TextColor3 = Color3.fromRGB(255,0,0)
				text.TextStrokeTransparency = 0
				text.TextStrokeColor3 = Color3.new(0,0,0)

				local hue = 0
				Run.RenderStepped:Connect(function()
					if text.Parent then
						hue = (hue + 1) % 360
						text.TextColor3 = Color3.fromHSV(hue/360,1,1)
					end
				end)
			end
		end
	end
end)
